Python version needed: 3.8.5
Install python-binance

And the following modules:
- math, numpy, pandas, datetime timedelta pandas, matplotlib
- sklearn, keras, seaborn 

++++++++++++++++++++++++++++++++++++

The file app_sos_badscaling is to be used in case the working version for the 3 coins fails.
This version if simply for the sake of showing that an output there is.

++++++++++++++++++++++++++++++++++++

Predictions are not very accurate.
In fact, models still need to be trained/compiled with larger 3min candles databases.
From those models, we would also extract the better fit scalers for each pair. 
